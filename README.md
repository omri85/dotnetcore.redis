# .NET core async Redis client exmaple
### Nuget:
This example use the wonderful async .net core redis client nuget package `Redis.NetCore`, install:
```bash
dotnet add package Redis.NetCore --version 0.8.8-beta
```
